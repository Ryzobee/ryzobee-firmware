dependencies:
  idf: ">=5.5,<6.0"
  # Product builds use an exact LVGL release. The public Lua Interface wraps
  # Ryzobee scene objects rather than exposing LVGL's C API directly.
  lvgl/lvgl: "==9.5.0"
